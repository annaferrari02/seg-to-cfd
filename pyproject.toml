[project]
name = "cfdpipe"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["PyYAML"]

[project.scripts]
cfdpipe = "cfdpipe.cli:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]